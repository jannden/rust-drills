Explore the differences between `String`, `string slice`, and `string literal` by implementing examples for each type.

**Plan:**
1. Create a mutable `String` and modify it by adding more text.
2. Create a `string slice` from a `String` and print the slice.
3. Use a `string literal` and print it.