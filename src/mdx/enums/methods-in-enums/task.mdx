Implement a method on the Pill enum that prints a message based on the variant.

**Plan:**
1. Implement a method called `take_action` on the Pill enum.
2. Inside the method, implement a match statement that prints a message based on the chosen variant.