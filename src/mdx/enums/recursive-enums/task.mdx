Create a recursive enum to represent a hierarchical structure.

**Plan:**
1. Define a recursive enum that can contain instances of itself.
2. Create an instance of this enum to represent a hierarchical structure.