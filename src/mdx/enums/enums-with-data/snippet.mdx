```rust
/* TODO: Create an enum with variants holding data */

fn main() {
  let agent = /* TODO: Assign a variant with data from the enum */
  match agent {
    /* TODO: Create a match arm for each variant, extracting and printing the data */
  }
}
```