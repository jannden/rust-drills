Implement error handling using the `Result` enum with custom error types to represent different error states.

**Plan:**
1. Create an enum to represent different error types.
2. Implement a function that returns a `Result` type, which can either be `Ok` with successful data or `Err` with one of the error types.
3. Use a match statement to handle the different error cases and print appropriate messages.