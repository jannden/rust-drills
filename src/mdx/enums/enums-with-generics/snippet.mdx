```rust
/* TODO: Define a generic enum with variants holding different data types */

fn main() {
  let neo: MatrixEntity<&str, &str> = /* TODO: Assign a variant of the generic enum */
  let smith: MatrixEntity<i32, &str> = /* TODO: Assign another variant of the generic enum */
}
```