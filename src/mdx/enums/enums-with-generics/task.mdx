Create a generic enum that can hold different data types and assign its variants.

**Plan:**
1. Define a generic enum with multiple variants that can hold different data types.
2. Create instances of the enum with specific data types.