Create an enum and assign one of its variants to a variable.

**Plan:**
1. Create an enum with multiple variants.
2. Assign one of the enum's variants to a variable.
3. Create a match statement to handle the different variants.