Use the `Option` enum to represent a variable that might contain a value or might be `None`.

**Plan:**
1. Create a variable of type `Option` and assign it either `Some` value or `None`.
2. Use a match statement to handle both `Some` and `None` variants, printing an appropriate message for each.