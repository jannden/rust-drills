Access elements in a vector using indexing and the `get` method.

**Plan:**
1. Access an element using indexing.
2. Access an element using the `get` method for safer access.
3. Print the accessed elements.