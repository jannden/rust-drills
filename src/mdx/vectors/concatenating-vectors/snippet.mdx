```rust
fn main() {
    let elves = vec!["Legolas", "Thranduil"];
    let dwarves = vec!["Gimli", "Thorin"];

    // TODO: Use the right method to merge the vectors into one
    let united_army = /* ... */;

    println!("United army: {:?}", united_army);
}
```