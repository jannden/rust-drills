```rust
fn main() {
  let your_vector = vec![/* TODO: Fill this with unique elements */];

  // Iterating over elements with their indexes
  for (index, element) in your_vector.iter().enumerate() {
    /* TODO: Perform an action that incorporates the element and its index */
  }
}

```