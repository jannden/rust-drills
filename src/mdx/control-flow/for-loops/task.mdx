Create your own `for` loop that iterates through a collection of items.

**Plan:**
1. Write the `for` loop that will iterate through this collection.
2. For each item in the collection, execute print a message.