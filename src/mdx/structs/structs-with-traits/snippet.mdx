```rust
#[derive(Debug)]
enum HouseState {
  AtPeace,
  AtWar,
}

struct House {
  name: String,
  region: String,
  bannermen: u32,
  state: HouseState,
}

/* TODO: Define a trait with methods representing common actions */

/* TODO: Implement the trait methods for the House struct */

fn main() {
  let mut house_baratheon = House {
      name: String::from("Baratheon"),
      region: String::from("The Stormlands"),
      bannermen: 800,
      state: HouseState::AtWar,
  };

  /* TODO: Call the trait methods on the instance of the struct */

}
```