Define a trait to encapsulate shared behaviors across different structs and implement it for a struct.

**Plan:**
1. Define a trait with methods representing common actions.
2. Implement the trait for the struct.
3. Create an instance of the struct and call the trait methods.