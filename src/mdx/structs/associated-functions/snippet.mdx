```rust
struct House {
  name: String,
  region: String,
  bannermen: u32,
}

/* TODO: Implement an associated function found_new_house for the House struct */
/* The associated function should create a new struct instance */

fn main() {
  let house_tarly = /* TODO: Use the associated function to create a new struct instance */

  println!("House {} of {} has been founded.", house_tarly.name, house_tarly.region);
}
```