Implement your own nested `for` loops.



**Plan:**
1. Create outer `for` loop iterating over `meal_times`.
2. Create inner `for` loop iterating over `foods`.
3. Inside the inner loop, use `println!` to consider which food to eat.

Start with the first step.