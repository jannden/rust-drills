Create your own `for` loop iterates through a collection of items.



**Plan:**
1. Create an array of strings that will represent your collection.
2. Write the initial statement for your `for` loop that will iterate through this collection.
3. For each item in the collection, execute print a message.

Start with the first step.