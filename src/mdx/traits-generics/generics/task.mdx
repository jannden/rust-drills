Define a generic struct and create instances of it with different types, then use its fields in the `main` function.

**Plan:**
1. Define a generic struct with a field of type `T`.
2. Create instances of the struct with different types.
3. Use the fields of these instances in the `main` function.