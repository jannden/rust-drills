Define a trait and implement it for different structures, then call the trait method on instances of these structures.

**Plan:**
1. Define a trait with a method (without a default implementation).
2. Implement the trait for different structures.
3. Create instances of the structures and call the trait method.
