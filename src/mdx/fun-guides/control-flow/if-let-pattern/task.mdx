Write your own `if let` statement in Rust that handles a specific pattern of an enum.

**Plan:**
1. In the `main` function, set a variable to one of the enum's variants.
2. Use `if let` to handle a specific pattern of the enum.