Write your own loop in Rust that utilizes the `continue` statement.

**Plan:**
1. Implement an `if` condition within your loop.
2. Inside the `if` condition, trigger the `continue` statement, thus skipping some iterations.
