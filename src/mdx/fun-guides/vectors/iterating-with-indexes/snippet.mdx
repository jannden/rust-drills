```rust
fn main() {
  let fellowship = vec!["Frodo", "Sam", "Merry", "Pippin"];

  // TODO: Iterate over the elements using a for loop with .enumerate()
  // TODO: Print a message that includes both the element and its index
}
```