```rust
fn main() {
    let mut army_of_men = vec!["Aragorn"];
    let elves_and_dwarves = vec!["Legolas", "Gimli"];

    // TODO: Use the right method to combine the vectors

    println!("United Army: {:?}", army_of_men);
}
```