Perform advanced string manipulation using Rust's string handling methods.

**Plan:**
1. Reverse a string using iterators and the `rev` method.
2. Capitalize each word in a string using iterators and closures.
3. Insert a character at a specific index in a string.