Define a function that returns a `Result` type to handle potential errors, and call it using a match statement.

**Plan:**
1. Define a function that returns a `Result` type, indicating success or an error message.
2. Call the defined function from within the `main` function, using a match statement to handle both the `Ok` and `Err` cases.
