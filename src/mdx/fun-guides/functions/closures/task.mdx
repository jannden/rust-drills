Define a closure that captures a variable from its environment and use it within a function.

**Plan:**
1. Define a closure that captures a variable from its surrounding environment.
2. Within the closure, compare force_strenght to the passed-in parameter.
