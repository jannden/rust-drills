```rust
fn analyze_plan(plan: &str) {
    println!("Analyzing borrowed plan: {}", plan);
}

fn main() {
    let heist_plan = String::from("The Heist Plan");

    // TODO: Borrow heist_plan temporarily
    // TODO: Pass the borrowed reference to analyze_plan

    // The original heist_plan wasn't consumed and can still be used
    println!("Original plan: {:?}", heist_plan);
}
```